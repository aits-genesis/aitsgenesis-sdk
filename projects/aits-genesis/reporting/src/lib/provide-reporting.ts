import { EnvironmentProviders, makeEnvironmentProviders } from '@angular/core';

/** Register reporting services. */
export function provideXReporting(): EnvironmentProviders {
  return makeEnvironmentProviders([]);
}
