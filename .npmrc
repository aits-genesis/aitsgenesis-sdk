# Publish and install @aits-genesis/* packages from GitHub Packages
@aits-genesis:registry=https://npm.pkg.github.com

# Consumers of this registry need a GitHub Personal Access Token
# with read:packages scope set as NODE_AUTH_TOKEN (or NPM_TOKEN).
# In GitHub Actions this is handled automatically via GITHUB_TOKEN.
